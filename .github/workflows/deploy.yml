name: Deploy to AWS Lambda

on:
  push:
    branches:
      - main
      - develop

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    # 환경변수 결정 로직 (따옴표로 감싸서 YAML 경고 방지)
    env:
      LAMBDA_NAME: "${{ github.ref == 'refs/heads/main' && secrets.PROD_LAMBDA_NAME || secrets.DEV_LAMBDA_NAME }}"
      DDB_TABLE: "${{ github.ref == 'refs/heads/main' && secrets.PROD_DDB_TABLE || secrets.DEV_DDB_TABLE }}"
      S3_BUCKET: "${{ github.ref == 'refs/heads/main' && secrets.PROD_S3_BUCKET || secrets.DEV_S3_BUCKET }}"
      API_ID: "${{ github.ref == 'refs/heads/main' && vars.AWS_API_ID || vars.DEV_AWS_API_ID }}"
      ENV_NAME: "${{ github.ref == 'refs/heads/main' && 'production' || 'development' }}"
      MENU_CACHE_ENABLED: "true"

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Utilities
        run: sudo apt-get install -y zip

      - name: Install Dependencies
        run: npm ci

      - name: Build Lambda Artifact
        run: npx tsx scripts/build-lambda.ts

      - name: Create Zip Package
        run: |
          cd dist
          zip -r ../function.zip .

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Deploy to Lambda
        run: |
          echo "Deploying to function: $LAMBDA_NAME"
          
          # 1. 코드 업데이트
          aws lambda update-function-code \
            --function-name "$LAMBDA_NAME" \
            --zip-file fileb://function.zip

          # 2. 환경변수 업데이트
          # 업데이트 충돌 방지를 위한 대기
          sleep 5

          echo "Updating configuration for: $LAMBDA_NAME"
          
          # Variables JSON 문자열을 한 줄로 구성 (공백 문제 방지)
          # AWS_REGION은 예약어이므로 제거 (Lambda 자동 주입)
          VARIABLES="Variables={DDB_TABLE_WAITING=$DDB_TABLE,S3_BUCKET=$S3_BUCKET,WAITING_SOURCE=ddb,MENU_SOURCE=s3,MENU_CACHE_ENABLED=$MENU_CACHE_ENABLED,NODE_ENV=$ENV_NAME}"
          
          aws lambda update-function-configuration \
            --function-name "$LAMBDA_NAME" \
            --environment "$VARIABLES"

      - name: Verify Deployment (Health Check)
        shell: bash
        run: |
          # API Gateway URL 구성 ($default 스테이지 사용으로 경로 불필요)
          API_URL="https://${API_ID}.execute-api.${{ vars.AWS_REGION }}.amazonaws.com"
          
          echo "Target API: $API_URL"
          MAX_RETRIES=5
          
          # Check paths in loop
          # /health (Lambda URL/API GW root) and /api/health (App router)
          for PATH_TO_CHECK in "/health" "/api/health"; do
            echo "----------------------------------------"
            echo "Checking $PATH_TO_CHECK ..."
            SUCCESS=false
            
            for ((i=1; i<=MAX_RETRIES; i++)); do
              # -L to follow redirects if any
              HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" -L "$API_URL$PATH_TO_CHECK")
              RESPONSE=$(curl -s -L "$API_URL$PATH_TO_CHECK")
              
              if [ "$HTTP_CODE" -eq 200 ]; then
                if echo "$RESPONSE" | grep -q '"status".*"ok"'; then
                  echo "✅ Success: $PATH_TO_CHECK (Attempt $i)"
                  SUCCESS=true
                  break
                else
                  echo "⚠️ Status 200 but content verify failed. Response: $RESPONSE"
                  echo "Retrying... ($i/$MAX_RETRIES)"
                fi
              else
                echo "⚠️ Failed: HTTP $HTTP_CODE. Retrying... ($i/$MAX_RETRIES)"
              fi
              
              if [ $i -lt $MAX_RETRIES ]; then
                sleep 5
              fi
            done
            
            if [ "$SUCCESS" = false ]; then
              echo "❌ Final Failure: $PATH_TO_CHECK after $MAX_RETRIES attempts"
              exit 1
            fi
          done
          
          echo "🎉 All checks passed!"
